# Application Settings
APP_NAME="Trellis Document Classifier"
DEBUG=False

# API Authentication
# Default key for local dev/review. Change this in production!
API_KEY=dev-secret-key

# Database
# Use localhost for local dev, or 'postgres' if running inside docker network
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/trellis

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
